name: Find large files
description: List the largest files under the current directory
command: "find . -type f -printf '%s %p\\n' | sort -nr | head -n {{count}}"
tags: [files, diagnostics]
args:
  - name: count
    description: Number of results
    default: "20"
