name: Temporary PR 31 resolver trigger

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "PR 31 resolution is handled by the temporary workflow on master"
